import Products from './products.json';

export const productState = { cart : 0 , cartProducts : [] , list : Products}

export const authState = false;

export const userInfo = { user : {}, authenticated : false };